# Upstash Redis (Vercel Marketplace → Upstash → "Redis"). Either naming works.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# KV_REST_API_URL=
# KV_REST_API_TOKEN=

# Secret used to sign run tokens (any long random string, e.g. `openssl rand -hex 32`).
RUN_SECRET=
